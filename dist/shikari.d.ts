export declare class Shikari {
    getCssSelector(document: HTMLDocument, element: HTMLElement): string;
    private getOptimalSelector(document, element, fullSelectorSequence);
    private fullSelectorSequenceToSelector(fullSelectors);
    private testSelector(document, element, selector);
    private getElementFullSelector(element);
    private getIdentRegex();
}
